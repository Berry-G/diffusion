<?php
/**
 * 이 기기에만 두는 설정.
 *
 * 이 파일을 config.local.php 로 복사해서 값을 채우세요.
 * config.local.php 는 .gitignore 에 들어 있어 저장소에 올라가지 않습니다.
 *
 * 관리자 비밀번호는 여기 두지 않습니다 — DB 에 해시로 저장하며,
 * 처음 한 번은 이 PC 에서 admin.php 로 들어가 정합니다.
 *
 * 기본값으로 잘 돌아간다면 이 파일은 아예 없어도 됩니다.
 */
return [
    // MariaDB 계정을 바꿨다면 여기서 덮어쓰세요.
    // 'db' => [
    //     'user' => 'root',
    //     'pass' => '',
    // ],

    // ComfyUI 를 다른 포트로 띄운다면.
    // 'comfy_url' => 'http://127.0.0.1:8000',
];
